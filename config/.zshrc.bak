# https://patorjk.com/software/taag/#p=display&f=ANSI%20Regular&t=Type%20Something%20
#
# ╔═══════════════════════════════════════════════════╗
# ║                                                   ║
# ║       ███████ ███████ ██   ██ ██████   ██████     ║
# ║          ███  ██      ██   ██ ██   ██ ██          ║
# ║         ███   ███████ ███████ ██████  ██          ║
# ║        ███         ██ ██   ██ ██   ██ ██          ║
# ║    ██ ███████ ███████ ██   ██ ██   ██  ██████     ║
# ║                                                   ║
# ╚═══════════════════════════════════════════════════╝

                                           
                                           
if [ -d "$HOME/scripts" ] ; then
  PATH="$PATH:$HOME/scripts"
fi

if [ -d "$HOME/.local/bin" ] ; then
  PATH="$PATH:$HOME/.local/bin"
fi

source "$ZINIT_HOME/zinit.zsh"
# export LC_CTYPE=C
# export LC_ALL=en_US.UTF-8
export LANG="en_US.UTF-8"
export LC_ALL="en_US.UTF-8"
export LC_COLLATE="C"
export LC_CTYPE="C"
export LC_MESSAGES="C"
export LC_MONETARY="C"
export LC_NUMERIC="C"
export LC_TIME="C"

# zinit light zsh-users/zsh-syntax-highlighting
# zinit light zsh-users/zsh-completions
# zinit light zsh-users/zsh-autosuggestions


# export NVIM_SCHEME="default_theme"
# # export ZSH="$HOME/.config/oh-my-zsh"
# export ZSH="$HOME/.oh-my-zsh"
# ZSH_THEME="robbyrussell"
# plugins=(
#     git
#     # zsh-z
#     # clipboard
#     # zsh-autosuggestions
#     # zsh-syntax-highlighting
#     zoxide
# )
#
# source $ZSH/oh-my-zsh.sh


eval "$(starship init zsh)"
# eval "$(zoxide init zsh)"

#
# source $ZSH/oh-my-zsh.sh
#
#
# # map CAPS LOCK to ctrl
# # use setting in /etc/default/keyboard instead
# # setxkbmap -option ctrl:nocaps
#
# [ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
#
#
# # vi mode
# bindkey -v
# export KEYTIMEOUT=10
#
# # Use vim keys in tab complete menu:
# bindkey -M menuselect 'h' vi-backward-char
# bindkey -M menuselect 'k' vi-up-line-or-history
# bindkey -M menuselect 'l' vi-forward-char
# bindkey -M menuselect 'j' vi-down-line-or-history
# bindkey 'jk' vi-cmd-mode
# bindkey -v '^?' backward-delete-char
#
# # Change cursor shape for different vi modes.
# function zle-keymap-select {
#   if [[ ${KEYMAP} == vicmd ]] ||
#      [[ $1 = 'block' ]]; then
#     echo -ne '\e[1 q'
#   elif [[ ${KEYMAP} == main ]] ||
#        [[ ${KEYMAP} == viins ]] ||
#        [[ ${KEYMAP} = '' ]] ||
#        [[ $1 = 'beam' ]]; then
#     echo -ne '\e[5 q'
#   fi
# }
# zle -N zle-keymap-select
# # zle-line-init() {
#     # zle -K viins # initiate `vi insert` as keymap (can be removed if `bindkey -V` has been set elsewhere)
#     # echo -ne "\e[5 q"
# # }
# # zle -N zle-line-init
# echo -ne '\e[5 q' # Use beam shape cursor on startup.
# preexec() { echo -ne '\e[5 q' ;} # Use beam shape cursor for each new prompt.
#
# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
# [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
#
# # bind zsh-autosuggestions to tab
# # bindkey '^I' autosuggest-accept
#
#
# alias y="zshz 2&>1"
# alias serve="python -m SimpleHTTPServer"
# alias status="mgitstatus ~/Repos/auxcontrol"
# alias space="echo \"Free space: \$(df -h | grep nvme | grep G | cut -d \" \" -s -f 3)\""
# alias gsa="mgitstatus -e $HOME/Repos/auxcontrol"
# alias gs="git status"
# alias grb="git rebase -i"
# alias gc="git commit"
# alias gA="git add -A"
# alias gd="git diff"
# alias gl="git log"
# alias server="sshpass -p 'root' ssh root@auxcontrol.local"
# alias cl="clear"
# alias aux="cd ~/Repos/auxcontrol"
# alias 2mon="xrandr --output eDP-1 --primary --output HDMI-1 --right-of eDP-1"
# alias vi="vim"
# alias dot="cd ~/Repos/dotfiles"
# alias conf="cd ~/.config"
# alias gg="cz"
# alias devkit="cd ~/Repos/auxcontrol/devkit/src"
# alias ffgh="firefox github.com/AuxControl/auxcontrol"
# alias ff88="firefox localhost:8080"
# alias ffaux="firefox localhost:8080"
# alias ff5="firefox localhost:5000"
# alias ff3="firefox localhost:3000"
# alias dot="vim ~/Repos/dotfiles"
# alias suspend="sudo /usr/sbin/pm-suspend"
# alias de="setxkbmap -layout de"
# alias us="setxkbmap -layout us"
# alias settings="gnome-control-center &> /dev/null &"
# # TODO improve
# alias watch-docs="cargo watch -s 'cargo doc && browser-sync reload'"
# alias sync-docs="browser-sync start --ss target/doc/ -s target/doc/ --directory --no-open"
# alias vim="nvim"
# alias nvim="~/.local/bin/nvim"
# alias light="ln -sf ~/.config/starship.light.toml ~/.config/starship.toml && alacritty-theme-switch -s light.yml && alias nvim='~/.local/bin/nvim -c \"colorscheme delek\"' && touch ~/.lightmode" 
# alias dark="ln -sf ~/.config/starship.dark.toml ~/.config/starship.toml && alacritty-theme-switch -s dark.yml && alias nvim='~/.local/bin/nvim' && rm -f ~/.lightmode"
# export EDITOR=vim
#
# # Find keycodes by running cat without any arguments then pressing the keys
# # 
# # This is F12
# bindkey -s '^[[24~' 'light^M'
# # This is F11
# bindkey -s '^[[23~' 'dark^M'
#
# function tmux-resize() {
#     echo "Use h,j,k,l to resize current tmux pane. q to exit."
#     while read -s -k1 op; do
#         case $op in
#             h)
#                 tmux resize-pane -L 2
#                 ;;
#             j)
#                 tmux resize-pane -D 1 
#                 ;;
#             k)
#                 tmux resize-pane -U 1 
#                 ;;
#             l)
#                 tmux resize-pane -R 2
#                 ;;
#             q|$'\e')
#                 break
#                 ;;
#             *)
#                 >&2 echo "unsupported $op"
#                 ;;
#         esac
#     done
# }
#
# if [ -f "$HOME/.lightmode" ] ; then
#   light 
# fi
